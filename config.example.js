var config = {
  db: {
    url: 'tcp://username:password@host:port/database',
    host: "host",
    database: "database",
    username: "username",
    password: "password"
  }
}
module.exports = config;